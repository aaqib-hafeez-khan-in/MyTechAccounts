# Maintenance Notes

## Profile directory

- Keep profile names, URLs, and categories in `index.html` synchronized with the accounts they represent.
- Prefer stable public profile URLs over temporary campaign or session links.
- After changing profile entries, verify the search/filter experience and the deployed GitHub Pages site.
- Keep the README live URL aligned with the active GitHub Pages deployment.
